apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-repo
  namespace: flux-system
spec:
  interval: 1m0s
  url: "https://github.com/manishdatick/git-ops.git"
  branch: main
  secretRef:
    name: flux-system
